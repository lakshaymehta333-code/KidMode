<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Brightness control -->
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <!-- System-wide grayscale (grant via: adb shell pm grant com.kidmode android.permission.WRITE_SECURE_SETTINGS) -->
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />
    <!-- Overlay for relaunching boring screen if minimized -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- Vibration feedback on wrong PIN -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KidMode">

        <!-- Setup / PIN configuration screen -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Full-screen boring overlay shown during Kid Mode -->
        <activity
            android:name=".BoringActivity"
            android:exported="false"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:taskAffinity="com.kidmode.boring"
            android:launchMode="singleTask"
            android:theme="@style/Theme.KidMode.Boring" />

        <!-- PIN entry to disable Kid Mode -->
        <activity
            android:name=".PinUnlockActivity"
            android:exported="false"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.KidMode.PinUnlock" />

        <!-- Quick Settings tile -->
        <service
            android:name=".KidModeTileService"
            android:exported="true"
            android:icon="@drawable/ic_tile"
            android:label="@string/tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Foreground service that manages Kid Mode session -->
        <service
            android:name=".KidModeService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Kid Mode screen restriction" />
        </service>

        <!-- Device admin receiver for lockNow() -->
        <receiver
            android:name=".KidModeDeviceAdminReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
